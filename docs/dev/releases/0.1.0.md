# Aptuni 0.1.0 release record

**Released:** 2026-09-21

**Release commit:** `33ea08a54a0a26a1dcfa74c419121afebe31f4c0`

**Tag:** `v0.1.0`

## Public channels

- PyPI: <https://pypi.org/project/aptuni/0.1.0/>
- GitHub Release: <https://github.com/ruihaomei/aptuni/releases/tag/v0.1.0>
- Source tag: <https://github.com/ruihaomei/aptuni/tree/v0.1.0>

No npm package or other registry artifact was published because the repository defines no
`package.json` or additional distribution surface.

## Hosted evidence

- Release workflow: <https://github.com/ruihaomei/aptuni/actions/runs/35619712980> — exact tag
  verification, full gate, locked audit, frozen evaluation, two byte-identical fixed-epoch builds,
  clean-wheel smoke, artifact upload, provenance generation and PyPI OIDC publication passed.
- Release-commit `main` CI: <https://github.com/ruihaomei/aptuni/actions/runs/35572245440> — macOS,
  Ubuntu/ext4 and build/supply-chain jobs passed.
- PyPI trusted publisher: GitHub repository `ruihaomei/aptuni`, workflow `release.yml`, environment
  `pypi`; no long-lived registry credential was used or stored.

## Artifact integrity

| Artifact | SHA-256 |
|---|---|
| `aptuni-0.1.0-py3-none-any.whl` | `f73fab2031c7a6562daa6731e628a1cb63e2788a791bb5e54c3e94c82f9c6819` |
| `aptuni-0.1.0.tar.gz` | `88aad7807e770d47a97e9c0e1afdf21bbd9fc37eab9e49c8959687935e36d556` |

A fresh macOS build from the exact tag, both Ubuntu workflow builds, the downloaded PyPI files and
the downloaded GitHub Release assets matched these digests byte for byte. The GitHub Release also
publishes `SHA256SUMS`.

## Public-install and safety verification

- A fresh Python 3.13 virtual environment installed `aptuni==0.1.0` strictly from PyPI; import and
  distribution versions were both `0.1.0` and resolved from site-packages, not the source tree.
- CLI/version/help passed, followed by a synthetic `init -> remember -> search -> context -> index
  status -> doctor` journey and a read-only local-only setup plan.
- The public `aptuni-mcp` install exposed the expected four tools; content-free health passed,
  personal access defaulted to denied, EOF shutdown was clean and the Internet/TCP canary failed
  closed with `aptuni_mcp_network_denied`.
- PyPI metadata reports Apache-2.0 and Python `>=3.13`. Wheel/sdist legal-file checks passed. The
  hosted audit covered 28 runtime dependencies with zero known vulnerabilities; its CycloneDX 1.5
  SBOM contains 30 components.
- Public package members and extracted text passed focused credential, private-key, private-path and
  dogfood-marker scans. `notes.md`, `task_plan.md`, credentials, private Vault content and raw host
  output are absent from the distributions.
- Public README, Chinese README, license, roadmap, security policy, logo, PyPI simple index and
  release/checksum links returned successfully.

## Independent decision

Review 50 independently inspected the public release and returned **APPROVE** with no blocking
findings: [`../reviews/50-release-final-review.md`](../reviews/50-release-final-review.md).
