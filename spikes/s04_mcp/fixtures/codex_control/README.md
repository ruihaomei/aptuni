# Codex project-config control fixture

This trusted control directory intentionally has no `.codex/` layer. S04 compares it with the
neighboring `codex_project` fixture under the same isolated `CODEX_HOME`.
